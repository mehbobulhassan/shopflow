import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Core colors
  static const primary = Color(0xFFFF6D00);
  static const secondary = Color(0xFF5A189A);

  static const background = Color(0xFFFFF8F2);
  static const surface = Color(0xFFFFFFFF);

  static const textPrimary = Color(0xFF240046);
  static const textSecondary = Color(0xFF6B6173);

  static const border = Color(0xFFE8DDEB);

  // Status colors
  static const success = Color(0xFF2E7D32);
  static const error = Color(0xFFD32F2F);
  static const warning = Color(0xFFFF9100);

  // Disabled colors
  static const disabledBackground = Color(0xFFE0DCE2);
  static const disabledText = Color(0xFF9B949F);
  static const disabledInputBackground = Color(0xFFF5F3F5);
}